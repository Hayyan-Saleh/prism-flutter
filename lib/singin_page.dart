import 'package:flutter/material.dart';

class SinginPage extends StatelessWidget {
  const SinginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Signin Page", style: TextStyle(fontSize: 25))),
    );
  }
}
